# GitHub 网页上传步骤指南

由于未安装 Git，可以直接通过 GitHub 网页上传。

## 📋 上传步骤

### 第一步：访问上传页面
打开浏览器，访问：
```
https://github.com/jj-s2/look-model
```

### 第二步：创建仓库（如果还没创建）
1. 点击右上角 "+" → "New repository"
2. Repository name: `look-model`
3. Description: `智慧养老监护平台 - AI视觉分析和毫米波雷达监护系统`
4. 选择 Public 或 Private
5. 不要勾选 "Initialize this repository with a README"
6. 点击 "Create repository"

### 第三步：上传文件

#### 方式 1：使用 Upload files 功能

**批次 1：上传根目录文件**
1. 在仓库页面点击 "uploading an existing file"
2. 拖拽以下文件：
   - README.md
   - .gitignore
   - UPLOAD_GUIDE.md
   - 如何上传到GitHub.md
   - 上传文件清单.txt
   - prepare_upload.py
   - upload_to_github.bat
   - _start_backend_direct.bat
   - _start_frontend_direct.bat
   - 直接启动（无需安装）.bat
   - 测试指南_查看前端效果.md
3. 在底部填写提交信息：`Add root files`
4. 点击 "Commit changes"

**批次 2：上传 backend 配置文件**
1. 点击 "Add file" → "Upload files"
2. 点击 "create new file"，创建路径：`backend/` (输入后按 /)
3. 回到上传页面，上传这些文件到 backend 目录：
   - backend/.env.example
   - backend/.gitignore
   - backend/requirements.txt
   - backend/create_tables.py
   - backend/seed_data.py
   - backend/seed_demo_data.py
   - backend/test_improvements.py
   - backend/生成演示数据.py
4. 提交信息：`Add backend config files`

**批次 3：上传 backend/app/**
由于文件较多，建议：
1. 点击 "Add file" → "Upload files"
2. 将整个 `backend/app/api/` 文件夹拖入
3. 提交信息：`Add backend API files`
4. 重复此步骤上传：
   - `backend/app/db/`
   - `backend/app/models/`
   - `backend/app/schemas/`
   - `backend/app/services/`
   - `backend/app/static/`
5. 单独上传 backend/app/ 下的文件：
   - __init__.py
   - main.py
   - config.py
   - dependencies.py

**批次 4：上传 backend/migrations/ 和 backend/tests/**
1. 上传 `backend/migrations/` 整个文件夹
2. 创建空文件夹 `backend/tests/`（点击 "Create new file"，输入 `backend/tests/.gitkeep`）

**批次 5：上传 frontend/**
1. 上传根文件：
   - frontend/index.html
   - frontend/package.json
   - frontend/package-lock.json
   - frontend/tsconfig.json
   - frontend/tsconfig.node.json
   - frontend/vite.config.ts

2. 上传文件夹：
   - `frontend/public/` (如果为空，创建 .gitkeep)
   - `frontend/src/hooks/`
   - `frontend/src/pages/`
   - `frontend/src/services/`
   - `frontend/src/types/`

3. 上传 frontend/src/ 根文件：
   - App.tsx
   - main.tsx
   - index.css

**批次 6：上传 deploy/**
1. 将整个 `deploy/` 文件夹拖入上传

**批次 7：上传 docs/**
1. 将整个 `docs/` 文件夹拖入上传

**批次 8：上传 mock/**
1. 将整个 `mock/` 文件夹拖入上传

**批次 9：上传 scripts/**
1. 将整个 `scripts/` 文件夹拖入上传

### 第四步：验证上传
1. 刷新仓库页面
2. 检查文件结构是否完整
3. 确认 README.md 正确显示

## ⚠️ 注意事项

### 不要上传的文件
- ❌ backend/venv/ (虚拟环境)
- ❌ backend/__pycache__/ (缓存)
- ❌ backend/.env (包含密钥)
- ❌ backend/*.db (数据库文件)
- ❌ frontend/node_modules/ (依赖包，太大)
- ❌ frontend/dist/ (构建输出)
- ❌ .vscode/ (IDE 配置)

### 文件数量限制
- 每次上传不要超过 100 个文件
- 单个文件不要超过 25 MB
- 如果提示文件太多，分批上传

### 文件夹上传技巧
1. 可以直接拖拽整个文件夹
2. GitHub 会自动保留目录结构
3. 空文件夹需要创建 .gitkeep 占位文件

## 🎯 快速上传建议

如果觉得网页上传太繁琐，建议：

### 选项 1：安装 Git（5分钟）
1. 下载：https://git-scm.com/download/win
2. 安装（使用默认选项）
3. 重启命令行
4. 运行：`.\upload_to_github.bat`

### 选项 2：使用 GitHub Desktop（最简单）
1. 下载：https://desktop.github.com/
2. 安装并登录
3. File → Add Local Repository → 选择项目文件夹
4. Commit → Publish Repository

## 📞 需要帮助？

如果上传过程中遇到问题：
1. 检查网络连接
2. 确认已登录 GitHub
3. 验证仓库权限
4. 查看错误提示信息

---

**建议**：如果计划长期使用 GitHub，强烈推荐安装 Git 或 GitHub Desktop，会大大提高效率！
