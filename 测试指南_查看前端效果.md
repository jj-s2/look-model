# 🎯 测试指南 - 查看前端效果

## 📋 操作步骤

### 1️⃣ 启动系统（如果还没启动）

双击运行：
```
直接启动（无需安装）.bat
```

系统会自动：
- 启动后端服务（http://localhost:8000）
- 启动前端服务（http://localhost:3000）
- 10秒后自动打开浏览器

### 2️⃣ 发送模拟跌倒风险事件

打开新的命令行窗口，执行：

```bash
cd 老人服务_2\老人服务_2
python scripts/send_complete_event.py --score 0.85
```

或者发送不同风险等级：
```bash
# 极高风险
python scripts/send_complete_event.py --score 0.95

# 中等风险
python scripts/send_complete_event.py --score 0.6

# 低风险
python scripts/send_complete_event.py --score 0.3
```

### 3️⃣ 前端查看效果

#### 📊 **仪表板页面** (http://localhost:3000/)
✅ 你会看到：
- **当前风险等级**: 显示 `HIGH`（红色大字）
- **今日告警**: 数字会增加
- 卡片会实时更新

#### 📈 **风险趋势页面** (导航栏点击 "风险趋势")
✅ 你会看到：
1. **选择设备下拉框**: 选择 `CAMERA_001`
2. **折线图**: 
   - 会出现新的数据点（风险评分 0.85）
   - 红色折线向上跳跃
3. **统计信息卡片**:
   - 最高风险分数: `0.85`
   - 数据点数增加

#### 🚨 **告警中心页面** (导航栏点击 "告警中心")
✅ 你会看到：
- 新增一条告警记录
- 风险等级: `HIGH`（红色标签）
- 状态: `NEW`（红色标签）
- 消息: "检测到高危跌倒风险！风险评分: 0.85"
- 可以点击 "确认" 按钮处理告警

## 🎬 效果演示流程

```
1. 打开浏览器 http://localhost:3000
   ↓
2. 记录当前的风险等级和告警数量
   ↓
3. 在命令行运行: python scripts/send_complete_event.py --score 0.85
   ↓
4. 刷新页面（或等待实时更新）
   ↓
5. 观察变化:
   - 仪表板风险等级变为 HIGH (红色)
   - 今日告警数量 +1
   - 风险趋势图出现新数据点
   - 告警中心出现新告警
```

## 🔧 如果没有看到效果

### 检查清单：

1. **后端是否运行？**
   ```bash
   # 访问此地址应该返回 JSON
   http://localhost:8000/health
   ```

2. **前端是否运行？**
   ```bash
   # 浏览器能否访问
   http://localhost:3000
   ```

3. **脚本是否成功？**
   ```bash
   # 运行脚本后应该看到 "✅ 风险记录创建成功"
   python scripts/send_complete_event.py --score 0.85
   ```

4. **数据是否写入？**
   ```bash
   # 直接查询 API
   http://localhost:8000/api/v1/risks/current
   http://localhost:8000/api/v1/alerts?limit=10
   ```

5. **刷新页面**
   - 按 F5 或点击浏览器刷新按钮
   - WebSocket 可能需要重新连接

## 📝 快速测试脚本

创建文件 `scripts/quick_test.py`:

```python
import requests
import time

print("🔍 测试前端数据显示...")

# 1. 发送高风险事件
print("\n1️⃣ 发送高风险事件...")
import subprocess
subprocess.run(["python", "scripts/send_complete_event.py", "--score", "0.85"])

time.sleep(1)

# 2. 验证数据
print("\n2️⃣ 验证后端数据...")
response = requests.get("http://localhost:8000/api/v1/risks/current")
print(f"当前风险: {response.json()}")

response = requests.get("http://localhost:8000/api/v1/alerts?limit=5")
print(f"最新告警数: {len(response.json())}")

print("\n✅ 完成！请打开前端页面查看: http://localhost:3000")
```

运行：
```bash
python scripts/quick_test.py
```

## 🎯 预期效果截图提示

### 仪表板
- 左上角 "在线设备" 卡片
- 左二 "当前风险等级" 卡片 → **HIGH** (红色)
- 左三 "今日告警" 卡片 → 数字增加

### 风险趋势
- 红色折线图最新点达到 0.85
- 统计信息显示最高风险分数 0.85

### 告警中心
- 表格第一行是新告警
- 红色 HIGH 标签
- 红色 NEW 状态
- 可以操作的按钮

## 💡 提示

- 多次运行脚本会累积数据
- 可以在告警中心确认和处理告警
- 风险趋势图会显示历史数据点
- 如果数据太多，可以清空数据库重新开始

## 🔄 重置数据（可选）

如果想清空数据重新测试：

```bash
cd backend
python create_tables.py  # 重建表
python seed_demo_data.py # 填充演示数据
```
