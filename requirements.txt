# Python 依赖清单
#
# 注意：本文件用于记录项目依赖。实际安装顺序很重要：
#   1. 先安装 PyTorch (CUDA 12.1 build)
#   2. 再安装 mmcv 预编译包 (cu121/torch2.4)
#   3. 最后安装 mmdet / mmpose / mmaction2
#
# 推荐使用 conda 创建 Python 3.10 环境（见 docs/env_compatibility_check.md）

# === PyTorch（需单独安装，指定 CUDA 12.1 build）===
# pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu121
torch>=2.4.0
torchvision>=0.19.0

# === OpenMMLab 工具链 ===
openmim>=0.3.7
# mmcv 与 mmengine 需通过 mim 安装预编译包：
#   mim install "mmengine>=0.7.2"
#   mim install "mmcv==2.2.0"
mmengine>=0.7.2
mmcv==2.2.0

# === OpenMMLab 项目库 ===
mmdet==3.3.0
mmpose==1.3.0
mmaction2==1.2.0

# === 视频与图像处理 ===
opencv-python>=4.8.0
decord>=0.6.0
imageio>=2.31.0
pillow>=10.0.0

# === 数据处理 ===
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0

# === 可视化 ===
matplotlib>=3.7.0
tqdm>=4.65.0
gradio>=5,<7

# === 配置与序列化 ===
pyyaml>=6.0
jsonschema>=4.17.0

# === HTTP 与下载 ===
requests>=2.31.0

# === 测试 ===
pytest>=7.4.0
pytest-cov>=4.1.0
