# Verified on Windows 11 + RTX 4060 Laptop GPU + NVIDIA driver 592.82.
# PyTorch ships the CUDA 12.1 runtime; a separate CUDA Toolkit is not required.
--index-url https://download.pytorch.org/whl/cu121
torch==2.4.1+cu121
torchvision==0.19.1+cu121

# Install the remaining project dependencies from the normal package index:
# python -m pip install -r requirements.txt --extra-index-url https://pypi.org/simple
